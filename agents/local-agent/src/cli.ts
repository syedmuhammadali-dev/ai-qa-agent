#!/usr/bin/env node
// Local Agent CLI entrypoint — connects a user's machine to the dashboard to
// run isolated, permissioned project execution (Phase 3).
//
// Not implemented yet. See RemainingTasks.md, Phase 3.

console.log(
  "ai-qa-agent local agent: not implemented yet (Phase 3). See RemainingTasks.md."
);
process.exit(1);
